#include "main.h"
#include <stdarg.h>
#include <stdio.h>
int _printf(const char *format, ...)
{
	char *_mv;
	char *s;
	unsigned int i;

	va_list arg;
	va_start(arg, format);

	*mv = format;

	for (i = 0; *_mv[i] != '\0'; i++)
	{
		while (*mv !='\0')
		{
			if (*mv[i] == '%')
				return (mv[i])
		}
	}
}
